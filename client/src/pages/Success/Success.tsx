import {Link} from 'react-router'

function Success() {
    return (
        <div>
            <p>Success</p>
            <Link to={'/'}>Back to Home</Link>
        </div>
    );
}

export default Success;
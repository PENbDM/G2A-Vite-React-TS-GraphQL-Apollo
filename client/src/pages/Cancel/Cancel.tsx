import {Link} from 'react-router'

function Cancel() {
    return (
        <div>
            <p>Cancel</p>
            <Link to={'/'}>Back to Home</Link>
        </div>
    );
}

export default Cancel;